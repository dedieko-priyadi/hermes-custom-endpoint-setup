# hermes-custom-endpoint-setup — Project Context

## 1. PROJECT IDENTITY

- Registry: `PRJ-2026-0192` — status `active`, progress `not_started`, maturity `idea`.
- Owner: the Registry `owner` field is empty. Do not infer a named owner.
- Local repository: `/home/dedieko/hermes-custom-endpoint-setup`.
- GitHub: https://github.com/dedieko-priyadi/hermes-custom-endpoint-setup.git (private unless verified otherwise).
- Branch/HEAD at context generation: `main` / `a259351` — re-verify before each work unit; this is a snapshot, not live state.

## 2. SYSTEM / BOUNDARY

The repository describes itself as follows (quoted from `README.md`):

> Deterministic guide to configure **Hermes Agent** (CLI + Desktop) to use a **custom LLM endpoint** (OpenAI-compatible base URL, e.g. a self-hosted gateway like 9router, vLLM, Ollama, LiteLLM, or any provider with an OpenAI-compatible API). **Goal**: friends install Hermes CLI → run one script → CLI is connected to the custom endpoint → **Hermes Desktop auto-inherits the same configuration** (no manual desktop setup). This guide is **replicable by both humans and machines**. A friend can simply ask their Hermes CLI to read this repository. ---

- This file records stable context only. It is not evidence that any described work was completed, approved, or delivered.
- Do not present a proposal, plan, or discussion as an approved decision.

## 3. AUTHORITY MAP

- Project Registry: structured project identity and lifecycle.
- Git repository: versioned project content, change record, and evidence.
- Drive: human-facing project documents.
- Kanban card: team coordination and status only — not a source of technical truth.
- Obsidian note: read-only navigation mirror; never the project authority.
- Reusable procedure: the matching skill.
- Credentials: the approved secret store only.
- Reconcile every surface through the stable ID `PRJ-2026-0192`.

## 4. ARCHITECTURE / GOVERNANCE INVARIANTS

- No credential value, API key, token, or password belongs in Git, this file, README, Drive, the kanban card, or any navigation surface.
- Verify live state before acting on it. A recorded value is evidence of a past lookup, not of current state.
- Do not claim a deployment, delivery, or acceptance from documentation alone; verify at the boundary the requirement crosses.
- Preserve source evidence; do not overwrite or delete it without separate explicit authorization.
- `AGENTS.md` changes only at a durable boundary and requires protected approval.

## 5. VERIFIED RESOURCE REFERENCES

- Registry ID: `PRJ-2026-0192`.
- Repository entry point: `README.md`.
- GitHub: https://github.com/dedieko-priyadi/hermes-custom-endpoint-setup.git

## 6. OPERATIONAL ENTRY POINTS

- Read `README.md` first; it is the project entry point and the checkpoint surface.
- `README.md` sections: How it works (1 minute mental model); Prerequisites; Setup Steps (deterministic); Verification checklist (both human & machine); Troubleshooting; Files; For your Hermes CLI (friend); Maintainer.
- No application build, deploy, or runtime procedure is defined in this repository. Do not invent one.

## 7. CONTINUITY POINTERS

- Stable identity: `PRJ-2026-0192` in the Project Registry.
- Current verified checkpoint: `README.md`.
- Stable project context: `AGENTS.md` (this file).

## 8. PERSISTENCE CLASSIFICATION RULE

- Stable project map / invariant / pointer -> `AGENTS.md`
- Latest verified checkpoint -> `README.md`
- Reusable procedure / pitfall -> skill
- Durable learned fact -> memory / Mnemosyne
- Detailed task progress -> Mnemosyne task slot / session mechanism
- Implementation history -> Git
- Live coordination state -> its authoritative system
- Temporary observation / raw log / easily rediscovered fact -> session/evidence only

## MANDATORY STARTUP AND CLOSEOUT

Before planning or execution, always read `README.md` and reconcile it with the authoritative
sources relevant to the requested work.

After completing and verifying a meaningful work checkpoint, update the `Current Verified Status`
section of `README.md` before the final project commit. Do not update it after every command.
